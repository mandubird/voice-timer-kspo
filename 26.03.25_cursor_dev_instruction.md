# Cursor AI 개발 지시 문서 — 목소리 타이머 v1

## 프로젝트 개요
- **앱명**: 목소리 타이머
- **목표**: 빠르게 출시해서 유료 전환 반응 확인
- **플랫폼**: 토스 미니앱 (웹앱, React + Vite + TypeScript + Tailwind)
- **결제**: 토스페이먼츠 SDK (일회성 결제, 2,900원)

---

## 핵심 원칙

> 기능 완성도보다 **첫 결제 발생** 검증이 우선이다.
> 범위를 넓히지 말고 아래 지시만 정확하게 구현한다.

---

## ✅ 완료된 작업 (건드리지 말 것)

- **작업 1**: VoiceSelector 3버튼 단순화 (기본 목소리 / 프리미엄 여성 ★Pro / 프리미엄 남성 ★Pro)
- **작업 2**: 홈 화면 카운트업, 양치 모드 숨김
- **작업 3**: audioPack 'none' 추가 + 무료 사용자 TTS fallback 구조
- **작업 4**: 결제 모달 문구 정리
- **작업 4.5**: 세션 화면 Pro 유도 UI
  - 세션 중: "🔒 Pro 목소리 미리듣기" 버튼 (무료 사용자만)
  - 세션 종료 후: Pro 업그레이드 CTA 카드 (무료 사용자만)
  - 인터벌: 라운드 진행 표시 (2 / 5라운드)
- **무료/유료 멘트 분리**: `proOnly: true` 옵션으로 멘트성 음성은 유료만 재생

---

## 현재 코드 구조 (참고용)

```
src/
  pages/
    HomePage.tsx         # 홈 (바로시작 카드 + 카운트다운/인터벌 메뉴)
    CountdownPage.tsx    # 카운트다운 설정
    IntervalPage.tsx     # 인터벌 설정
    RunSessionPage.tsx   # 타이머 실행 화면
  store/
    settingsStore.ts     # isPro, audioPack, openPaywall, setAudioPack 등
  utils/
    audioPlayer.ts       # playAudioFile / playOrSpeak (proOnly 옵션 있음)
  components/
    VoiceSelector.tsx    # 3버튼 목소리 선택
    PaywallModal.tsx     # 결제 모달 (현재 결제 로직 없음 → 이번에 구현)

public/audio/
  default/               # 프리미엄 여성 wav (audioPack='default')
  energy/                # 프리미엄 남성 wav (audioPack='energy')
```

### 핵심 상태 (settingsStore)
```typescript
isPro: boolean
audioPack: 'none' | 'default' | 'energy'
setPro: (isPro: boolean) => void         // isPro 저장 (localStorage 포함)
setAudioPack: (pack: AudioPack) => void  // audioPack 저장
openPaywall: (featureLabel?: string) => void
```

---

## 🔴 남은 작업

### 작업 5: 토스페이먼츠 결제 연동

#### 환경 설정
```bash
npm install @tosspayments/tosspayments-js
```

`.env` 파일에 추가:
```
VITE_TOSS_CLIENT_KEY=test_ck_여기에_클라이언트_키
```

#### 구현 위치
`src/components/PaywallModal.tsx` — 현재 결제 버튼이 있지만 실제 결제 로직이 없음.

#### 구현 흐름
```
1. PaywallModal에서 "Pro 시작하기" 버튼 클릭
2. TossPayments(clientKey) 인스턴스 생성
3. tossPayments.requestPayment('카드', {
     amount: 2900,
     orderId: 'voice-timer-pro-' + Date.now(),
     orderName: '목소리 타이머 Pro',
     customerName: '사용자',
     successUrl: window.location.origin + '/payment/success',
     failUrl: window.location.origin + '/payment/fail',
   })
4. /payment/success 페이지에서:
   - URL 파라미터(paymentKey, orderId, amount) 확인
   - setPro(true) 호출 → localStorage 저장
   - setAudioPack('default') → 프리미엄 여성 기본값
   - 홈으로 이동 + 성공 토스트
5. /payment/fail 페이지에서:
   - 실패 메시지 표시 후 홈으로 이동
```

#### 라우터에 추가
```typescript
// src/App.tsx 또는 router 파일
<Route path="/payment/success" element={<PaymentSuccessPage />} />
<Route path="/payment/fail" element={<PaymentFailPage />} />
```

#### PaymentSuccessPage 구현
```typescript
// src/pages/PaymentSuccessPage.tsx
// URL params: paymentKey, orderId, amount
// 서버 검증 없이 클라이언트에서 바로 isPro = true 처리 (v1 단순화)
// setPro(true) + setAudioPack('default') + navigate('/')
```

#### 주의사항
- v1은 서버 검증 없이 localStorage만 사용 (출시 속도 우선)
- 테스트 키(`test_ck_...`)로 개발, 실제 키(`live_ck_...`)로 배포
- 토스페이먼츠 대시보드에서 successUrl/failUrl 도메인 등록 필요

---

### 작업 6: 토스 미니앱 배포 설정

토스 미니앱은 일반 웹앱과 동일하게 배포 후 URL 등록.

#### 필요한 것
1. Vercel/Netlify 등 정적 호스팅 배포
2. 토스 개발자센터에서 미니앱 등록
3. successUrl/failUrl 도메인 토스페이먼츠에 등록

---

## 하지 말아야 할 것

- 서버 구축 금지 (v1은 정적 배포)
- 구독형/정기결제 금지 (일회성 2,900원)
- 영수증 발송, 환불 로직 금지 (v1)
- 이미 완료된 작업 1~4 수정 금지
- 숫자 wav 파일 관련 작업 금지 (별도 진행 중)
