# Voice Coach Timer UI/UX 개선 가이드

이 문서는 **토스 인앱용 Voice Coach Timer**의 UI/UX 개선 방향을 정리한 문서입니다.

포함 내용:
- 클로드/커서에 바로 붙여넣는 **수정 지시문**
- 반드시 같이 넣을 **중요 문장**
- 추가로 꼭 넣어야 할 **2가지 개선 포인트**
- **홈 / 설정 / 실행 화면별** 문구, 컬러, 컴포넌트 구조 가이드

---

## 1. 최종 수정 지시문 (복붙용)

아래 텍스트를 그대로 복사해서 붙여넣으면 됩니다.

```text
아래 프로젝트의 UI를 수정해줘.
중요: 기능 로직, 상태값, 이벤트 핸들러, 데이터 구조, 라우팅은 건드리지 말고 UI/스타일만 개선해줘.

목표
- 전체 톤은 “토스 인앱에 어울리는 깔끔함 + 즉시 실행감 + 약간의 에너지”
- 현재 문제는 너무 폼(form) 같고, 카드들이 다 똑같아 보여서 기능 차별성이 약함
- 사용자가 첫 화면에서 “아, 이건 운동/양치할 때 바로 쓰는 음성 타이머구나”를 즉시 이해해야 함

수정 요구사항

1) RunSessionPage 개선
- 타이머 숫자를 화면의 주인공으로 만들어줘
- 숫자 영역이 화면 높이의 절반 이상 체감되게 구성해줘
- 숫자 font-size는 clamp(120px, 40vw, 200px) 수준으로 크게 적용
- font-weight는 800~900 수준
- line-height는 최대한 타이트하게
- 숫자는 화면 중앙에 강하게 배치
- "초" 단위는 숫자 오른쪽 아래에 작게 붙여서 subscript 느낌으로 배치
- 지금처럼 작은 정보 조각처럼 보이지 않게, 숫자 자체가 메인 비주얼이 되게 해줘
- 빈 공간이 허전하지 않도록 아주 약한 배경 그라디언트나 소프트 글로우를 넣어도 됨
- 단, 과한 게임 UI 말고 미니앱 수준의 절제된 에너지로 유지
- 버튼은 하단 고정 또는 시선이 자연스럽게 이어지는 위치로 정리
- 운동 중 화면답게 긴장감/집중감이 느껴지도록 정보 위계를 재구성해줘

2) 홈 화면 카드 4개 차별화
- 카드 4개가 지금 너무 똑같이 보여서 모드 구분이 약함
- 각 카드에 서로 다른 배경색 또는 아주 약한 그라디언트를 적용해줘
- 컬러 가이드:
  - 카운트다운: 진한 파랑 계열
  - 카운트업: 퍼플 계열
  - 인터벌: 레드/오렌지 계열
  - 양치: 하늘색 계열
- 카드마다 아이콘 크기를 40px 이상으로 키워줘
- 제목/설명/아이콘의 위계를 더 분명히 해줘
- 카드 전체를 눌러보고 싶게 보이도록, 그림자/라운드/배경 대비를 조정해줘
- 단, 토스 느낌을 해치지 않게 과한 장식은 금지
- 홈 첫인상은 “기능 목록”이 아니라 “바로 시작할 프리셋 선택”처럼 보여야 함

3) 설정 화면 개선
- 설정 화면이 현재 너무 폼(form) 같아서 운동 앱 느낌이 약함
- 진동/소리 체크박스를 토글 스위치(toggle switch) UI로 교체해줘
- 알림 옵션 카드 3개 높이를 통일해서 정렬감을 맞춰줘
- 전체적으로 입력 폼이 아니라 선택형 옵션 카드처럼 보이게 개선
- 선택된 버튼/카드는 더 선명하게 활성 상태가 보이게 해줘
- 비선택 상태는 심심하지 않되 과도하게 흐리지 않게 정리
- 운동 앱 느낌이 나도록 조금 더 즉시적이고 가벼운 선택 UI로 바꿔줘

4) 공통 스타일 가이드
- 전체는 미니멀하고 깨끗하게
- 토스 미니앱 느낌의 명확한 정보 위계
- 라운드, 여백, 그림자는 통일감 있게 정리
- 과한 네온/게임풍 금지
- "깔끔하지만 심심하지 않은" 방향
- 모바일 화면 기준으로 최적화
- 접근성 고려해서 텍스트 대비 유지
- 버튼/카드/토글의 터치 영역 충분히 확보

5) 추가로 같이 손봐줘
- 홈 화면 상단 타이틀은 지금 너무 일반 텍스트 같음
- 브랜드 헤더처럼 보이도록 타이틀/서브카피/여백 구조를 개선해줘
- 단, 새 기능 추가보다 기존 구조를 더 좋아 보이게 다듬는 데 집중
- 전체 결과물은 “디자이너가 손본 소비자용 앱 화면”처럼 보여야 함

작업 방식
- 먼저 현재 구조를 유지한 채 스타일만 개선
- 가능하면 Tailwind 또는 현재 스타일 시스템 기준으로 바로 반영 가능한 코드로 수정
- 수정 후 각 화면별 변경 포인트를 짧게 설명
```

---

## 2. 반드시 같이 넣을 중요 문장

```text
중요:
이 앱은 개발자용 도구가 아니라 일반 사용자가 토스 안에서 바로 실행하는 소비자형 미니앱이다.
따라서 “설정 화면”처럼 보이기보다 “즉시 시작 가능한 프리셋 UI”처럼 보여야 한다.
특히 RunSessionPage는 숫자가 가장 중요하며, 사용자는 운동 중 멀리서도 시간을 인식해야 한다.
```

---

## 3. 추가로 꼭 넣어야 할 2가지

### 첫째. 홈 메인 카피를 영어 중심이 아니라 한국어 혜택형 문장으로 바꿔라

지금 `Voice Coach Timer`만 크게 보이면 기능은 알겠지만, **왜 써야 하는지**는 바로 안 들어옵니다.
토스 인앱에서는 첫 화면에서 **혜택이 바로 읽혀야** 전환이 좋습니다.

추천 방향:
- 보지 않아도 되는 음성 타이머
- 운동할 때 화면 안 봐도 되는 타이머
- 남은 시간을 목소리로 알려주는 타이머
- 반복 횟수와 남은 시간을 음성으로 코치하는 타이머

권장 구조:
- 1줄 헤드라인: 기능 혜택
- 1줄 서브카피: 언제 쓰는지 설명

예시:
- **보지 않아도 되는 음성 타이머**
  - 운동할 때 남은 시간과 횟수를 목소리로 알려줘요
- **운동할 때 화면 안 봐도 되는 타이머**
  - 카운트다운, 카운트업, 인터벌, 양치 모드를 바로 시작하세요

### 둘째. 홈 상단에 대표 프리셋 1~2개를 크게 노출해라

토스 인앱은 설정이 많은 앱보다 **바로 눌러서 실행되는 앱**이 더 잘 먹힙니다.
그래서 홈에서 기능 카드만 나열하는 것보다, **대표 사용 시나리오를 먼저 크게 보여주는 구조**가 좋습니다.

추천 프리셋:
- 60초 카운트다운 시작
- 3분 양치 시작
- 30초 운동 / 10초 휴식 인터벌 시작

권장 배치:
1. 브랜드/혜택 헤더
2. 퀵 스타트 버튼 1~2개
3. 모드 카드 4개
4. 하단 CTA 또는 최근 사용 프리셋

---

## 4. 전체 UI/UX 방향 요약

이 앱은 본질적으로 아래 성격을 가져야 합니다.

- 복잡한 설정 앱이 아니라 **즉시 실행 앱**
- 폼 기반 도구가 아니라 **소비자형 미니앱**
- 운동 중 / 양치 중 / 손 못 쓰는 상황에서 쓰는 앱
- 멀리서도 알아보이는 정보 위계
- 토스 안에서도 이질감 없는 깔끔함

추천 비율:

**깔끔함 70 + 즉시 실행감 20 + 에너지 10**

너무 심심하면 안 되고, 너무 운동앱/게임앱처럼 과하면 토스 인앱 감성과 안 맞습니다.

---

## 5. 화면별 구체 가이드

# 5-1. 홈 화면 가이드

### 목표
홈은 “메뉴 목록”이 아니라 아래처럼 느껴져야 합니다.

- 아, 이건 음성 타이머 앱이구나
- 바로 시작하면 되겠네
- 운동할 때 쓰기 편하겠다

즉, 홈의 역할은 **설명**이 아니라 **즉시 실행 유도**입니다.

### 추천 문구

#### 헤더 후보 A
- **보지 않아도 되는 음성 타이머**
- 운동할 때 남은 시간과 횟수를 목소리로 알려줘요

#### 헤더 후보 B
- **운동할 때 화면 안 봐도 되는 타이머**
- 카운트다운, 인터벌, 양치 코치까지 한 번에

#### 헤더 후보 C
- **목소리로 알려주는 운동 타이머**
- 남은 시간, 횟수, 구간 전환을 음성으로 코치해요

#### 브랜드 표기 추천
- 작은 캡션: `Voice Coach Timer`
- 큰 제목: 한국어 혜택형 문장
- 그 아래 서브카피

예:
- Voice Coach Timer
- **보지 않아도 되는 음성 타이머**
- 운동할 때 남은 시간과 횟수를 목소리로 알려줘요

### 컬러 방향
토스 인앱 느낌을 유지하려면, 카드 색은 **진하지만 채도가 과하지 않게** 잡는 게 좋습니다.

- 카운트다운
  - 메인: `#2F6BFF`
  - 보조: `#EAF1FF`
  - 그라디언트: `linear-gradient(135deg, #2F6BFF 0%, #5A8BFF 100%)`

- 카운트업
  - 메인: `#7A5CFA`
  - 보조: `#F1EDFF`
  - 그라디언트: `linear-gradient(135deg, #7A5CFA 0%, #9A7BFF 100%)`

- 인터벌
  - 메인: `#FF6A3D`
  - 보조: `#FFF0EA`
  - 그라디언트: `linear-gradient(135deg, #FF6A3D 0%, #FF8A3D 100%)`

- 양치
  - 메인: `#55B8FF`
  - 보조: `#EAF8FF`
  - 그라디언트: `linear-gradient(135deg, #55B8FF 0%, #8ED6FF 100%)`

주의:
- 카드 전체를 너무 진하게 칠하지 말 것
- 연한 바탕 + 강조 포인트 컬러 구조 추천
- 텍스트 대비는 확실히 유지

### 컴포넌트 구조
추천 순서:

1. 상단 브랜드/혜택 헤더
2. 퀵 스타트 버튼 2개
3. 모드 카드 4개
4. 하단 CTA

#### 퀵 스타트 예시
- `60초 바로 시작`
- `양치 3분 시작`

#### 모드 카드 예시 문구
- 카운트다운: 남은 시간을 음성으로 안내
- 카운트업: 반복 횟수를 음성으로 세어드려요
- 인터벌: 운동 · 휴식 구간을 코치
- 양치 코치: 3분 양치 루틴을 음성으로 안내

#### 카드 구조
- 큰 아이콘 (40~48px 이상)
- 모드명
- 1줄 설명
- 눌림 affordance

---

# 5-2. 설정 화면 가이드

### 목표
설정 화면은 입력 폼처럼 보이면 안 됩니다.
이 화면의 목표는 **빠르게 선택하고 바로 시작하게 만드는 것**입니다.

즉, 느낌은 아래에 가까워야 합니다.
- 옵션 설정 화면 X
- 즉시 실행 직전 컨트롤 패널 O

### 추천 문구
짧고 바로 이해되는 라벨이 좋습니다.

- 시간
- 알림 방식
- 진동
- 소리
- 음성 톤
- 시작하기

알림 예시:
- 매 초 안내
- 10초마다 안내
- 마지막 5초만

톤 예시:
- 차분한 톤
- 에너지 톤 (Pro)

보조 문구 예시:
- 다양한 목소리와 톤은 Pro에서 순차적으로 추가됩니다.
- 운동 스타일에 맞는 음성 톤을 선택해보세요.

### 컬러 방향
설정 화면은 홈보다 덜 화려해야 합니다.
대신 **선택 상태가 확실히 보이는 UI**가 중요합니다.

- 기본 보더: `#E8EAF0`
- 선택 보더: `#2F6BFF`
- 선택 배경: `#F3F7FF`
- 본문 텍스트: `#111111`
- 보조 텍스트: `#7A7F8A`

권장 스타일:
- 페이지 배경: 거의 흰색
- 카드 배경: 아주 연한 회색 또는 연한 블루 틴트
- 선택 상태: 보더 + 배경 + 텍스트 모두 명확하게 변화
- 비선택 상태: 너무 흐리지 않게 정리

### 컴포넌트 구조
1. 상단 바
   - 뒤로가기
   - 현재 모드명

2. 시간 선택
   - 동일 높이 버튼 그룹
   - 예: 30초 / 45초 / 60초 / 90초

3. 알림 방식 카드
   - 카드 3개 높이 완전 통일
   - 보조 문구 길이도 되도록 맞춤
   - 선택형 탭처럼 보이게 구성

4. 토글 영역
   - 진동 / 소리 체크박스를 스위치 토글로 교체
   - 같은 줄 높이, 같은 폭으로 정렬

5. 음성 선택
   - 성별 / 톤을 카드 버튼처럼 구성
   - Pro 옵션은 잠금 아이콘 또는 뱃지로 표현

6. 하단 CTA
   - `시작하기`
   - 넓고 강한 버튼

### 세부 개선 포인트
- 진동/소리 체크박스는 무조건 토글 스위치로 변경
- 알림 카드 높이 들쭉날쭉 금지
- 선택 상태는 보기만 해도 구분되게
- 전체 화면이 “폼 작성”이 아니라 “빠른 선택”처럼 보여야 함

---

# 5-3. 세션 실행 화면 가이드

### 목표
세션 실행 화면은 **숫자가 전부**입니다.
이 화면에서 사용자는 텍스트를 읽는 게 아니라, **멀리서 숫자를 인식**해야 합니다.

즉, 느낌은 아래여야 합니다.
- 기능 설명 화면 X
- 운동 중 실제로 보는 런타임 화면 O

### 추천 문구
실행 화면에서는 문구를 최소화해야 합니다.

상단 보조 정보 예시:
- 카운트다운
- 10초마다 안내 중
- 차분한 여성 음성

버튼 예시:
- 일시정지
- 다시 시작
- 종료

중요:
- 실행 중에는 문구보다 숫자가 우선
- 부가 정보는 최대한 작게

### 컬러 방향
실행 화면은 홈/설정보다 조금 더 몰입감 있게 가도 됩니다.
단, 과한 네온/게임풍은 금지입니다.

추천:
- 배경: 화이트 또는 아주 연한 블루/그레이 그라디언트
- 숫자: 거의 블랙 또는 딥 네이비
- 강조 포인트: 블루 계열
- 긴장감용 아주 약한 글로우 또는 배경 원형 하이라이트 가능

예:
- 배경: `linear-gradient(180deg, #F8FBFF 0%, #EEF4FF 100%)`
- 숫자: `#101828`
- 포인트: `#2F6BFF`

### 컴포넌트 구조
추천 구조:

1. 상단 최소 정보
   - 현재 모드
   - 현재 안내 방식

2. 중앙 초대형 숫자
   - `font-size: clamp(120px, 40vw, 200px)`
   - `font-weight: 800~900`
   - line-height 타이트
   - 화면 절반 이상을 차지하는 체감

3. 단위 표기
   - `초`는 숫자 우측 하단에 작게
   - 서브스크립트처럼 붙이기
   - 숫자보다 존재감 낮게

4. 하단 액션
   - 일시정지 / 종료 또는 메인 버튼 1~2개
   - 하단 고정 또는 자연스러운 하단 정렬

### 세부 개선 포인트
- 숫자는 지금보다 훨씬 커져야 함
- 숫자 자체가 메인 비주얼이어야 함
- 빈 공간은 완전히 비워두지 말고, 아주 약한 배경 에너지로 채우기
- 운동 중 화면답게 집중감이 있어야 함
- "88초"처럼 조그맣게 보이는 인상은 절대 금지

---

## 6. 클로드에게 추가로 주면 좋은 한 줄

```text
이 앱은 “설정을 잘 만드는 앱”이 아니라 “토스 안에서 바로 실행되는 소비자형 타이머 앱”처럼 보여야 한다. 특히 홈은 즉시 시작, 설정은 빠른 선택, 실행 화면은 초대형 숫자 중심으로 재구성해줘.
```

---

## 7. 추천 우선순위

1. **RunSessionPage 숫자 확대**
2. **홈 카드 4개 차별화**
3. **설정 화면 체크박스 → 토글 스위치 변경**
4. **홈 헤더 카피를 혜택형 한국어 문장으로 수정**
5. **홈 퀵 스타트 1~2개 추가**

이 순서로 수정하면 체감 개선이 가장 큽니다.
