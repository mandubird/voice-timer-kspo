# 음성 파일(mp3) 시스템 개발 지시서 v2

## 배경 및 목적

현재 앱은 Web Speech API(실시간 TTS)로 음성을 재생한다.
이 방식은 기기마다 목소리가 다르고, iOS Safari에서 제약이 있으며, 품질 통제가 불가능하다.

**목표:** 사전 생성된 mp3 파일을 우선 재생하고, 파일이 없으면 기존 TTS로 자동 fallback하는 구조를 만든다.
mp3 파일이 없어도 앱은 지금처럼 동작해야 한다. 파일만 넣으면 자동으로 더 좋은 목소리로 바뀐다.

---

## 목소리 로드맵

### v1 — 여성 부드러운 톤 1개 (파일 30개)
| 서비스 | 추천 목소리 |
|--------|-------------|
| Google TTS | `ko-KR-Neural2-A` |
| Google 대안 | `ko-KR-Wavenet-A` |
| Azure | `ko-KR-SunHiNeural` 또는 `ko-KR-JiMinNeural` |

### v1.5 — 남성 에너지 톤 추가 (파일 60개)
| 서비스 | 추천 목소리 |
|--------|-------------|
| Google TTS | `ko-KR-Neural2-C` |
| Azure | `ko-KR-HyunsuNeural` 또는 `ko-KR-InJoonNeural` |

### v2 — 4가지 톤으로 확장 (파일 120개)
- `soft` — 여성 부드러움
- `energy` — 남성 에너지
- `kids` — 아이용 밝은 톤
- `calm` — 차분한 중성 톤

**확장 원칙:** 문구를 늘리는 게 아니라 같은 30개 문구에 목소리만 복제
```
30문구 × 1목소리 = 30파일   (v1)
30문구 × 2목소리 = 60파일   (v1.5)
30문구 × 4목소리 = 120파일  (v2)
```

---

## 파일명 규칙

```
[category]_[event]_[tone]_[index].mp3
```

> tone을 파일명에 포함하는 이유: 나중에 목소리 추가 시 기존 파일명과 충돌 없이 확장 가능

---

## v1 스타터 파일 30개 (soft 톤)

### 1) 공통 안내 10개
| 파일명 | 텍스트 |
|--------|--------|
| `common_start_soft_01.mp3` | 시작해볼게요. |
| `common_ready_soft_01.mp3` | 준비되셨죠? |
| `common_3sec_soft_01.mp3` | 3초 뒤 시작합니다. |
| `common_half_soft_01.mp3` | 절반 왔어요. |
| `common_10sec_soft_01.mp3` | 10초 남았어요. |
| `common_5sec_soft_01.mp3` | 5초 남았어요. |
| `common_last_soft_01.mp3` | 거의 다 왔어요. |
| `common_end_soft_01.mp3` | 종료됐어요. |
| `common_restart_soft_01.mp3` | 다시 시작해볼게요. |
| `common_done_soft_01.mp3` | 한 세트 완료예요. |

### 2) 운동용 8개
| 파일명 | 텍스트 |
|--------|--------|
| `workout_push_energy_01.mp3` | 좋아요, 그대로 갑니다. |
| `workout_push_energy_02.mp3` | 조금만 더 버텨볼게요. |
| `workout_push_energy_03.mp3` | 잘하고 있어요. |
| `workout_push_energy_04.mp3` | 리듬 좋아요. |
| `workout_push_energy_05.mp3` | 힘들어도 한 번 더 가요. |
| `workout_round_energy_01.mp3` | 다음 라운드 시작합니다. |
| `workout_hold_energy_01.mp3` | 그대로 유지해요. |
| `workout_finish_energy_01.mp3` | 운동 구간 끝났어요. |

### 3) 휴식용 6개
| 파일명 | 텍스트 |
|--------|--------|
| `rest_start_soft_01.mp3` | 쉬는 시간이에요. |
| `rest_breathe_soft_01.mp3` | 숨 한번 고를게요. |
| `rest_half_soft_01.mp3` | 휴식도 절반 지났어요. |
| `rest_10sec_soft_01.mp3` | 곧 다시 시작해요. |
| `rest_ready_soft_01.mp3` | 준비해주세요. |
| `rest_end_soft_01.mp3` | 쉬는 시간 끝, 다시 갑니다. |

### 4) 양치용 6개
| 파일명 | 텍스트 |
|--------|--------|
| `brush_start_kids_01.mp3` | 양치 시작해볼까요? |
| `brush_top_kids_01.mp3` | 위쪽부터 닦아볼게요. |
| `brush_bottom_kids_01.mp3` | 아래쪽도 닦아볼까요? |
| `brush_left_kids_01.mp3` | 왼쪽도 깨끗하게요. |
| `brush_right_kids_01.mp3` | 오른쪽도 닦아볼게요. |
| `brush_end_kids_01.mp3` | 우와, 양치 끝! |

### 5) 추임새/리액션 6개 (Pro 전용)
| 파일명 | 텍스트 |
|--------|--------|
| `reaction_cheer_soft_01.mp3` | 좋아요. |
| `reaction_cheer_soft_02.mp3` | 오, 잘하고 있어요. |
| `reaction_cheer_energy_01.mp3` | 좋았어요, 한 번 더! |
| `reaction_cheer_energy_02.mp3` | 지금 흐름 좋아요. |
| `reaction_kids_01.mp3` | 와, 정말 잘했어요! |
| `reaction_kids_02.mp3` | 반짝반짝 깨끗해지고 있어요. |

---

## 구현 사항

### 1. `src/utils/audioPlayer.ts` 신규 생성

```typescript
/**
 * mp3 파일을 재생한다. 파일이 없거나 재생 실패 시 false를 반환한다.
 */
export async function playAudioFile(filename: string): Promise<boolean>

/**
 * mp3 우선 재생, 실패 시 TTS fallback.
 * speakFn은 현재 useVoice의 speak()를 그대로 전달한다.
 */
export async function playOrSpeak(
  filename: string,
  fallbackText: string,
  speakFn: (text: string, opts?: { interrupt?: boolean }) => void,
  opts?: { interrupt?: boolean }
): Promise<void>
```

#### 구현 상세
- 음성 파일 경로: `/audio/{filename}`
- `new Audio(path)` 로 재생
- `audio.play()` 가 reject되면 false 반환 (파일 없음 또는 브라우저 제한)
- 에러는 조용히 처리 (console.error 금지, UI 노출 금지)
- interrupt가 true이면 현재 재생 중인 오디오를 먼저 정지 후 재생
- 현재 재생 중인 Audio 인스턴스를 모듈 스코프 변수로 관리

#### 동작 원리
```
playOrSpeak('rest_start_soft_01.mp3', "쉬는 시간이에요.", speak)
  ↓
/audio/rest_start_soft_01.mp3 존재? → 재생 ✅
  ↓ 없거나 실패
speak("쉬는 시간이에요.")  ← 기존 Web Speech API TTS
```

---

### 2. `src/data/voiceFiles.ts` 신규 생성

코드에서 파일명을 하드코딩하지 않도록 중앙 매핑 파일을 만든다.

```typescript
export const voiceFiles = {
  common: {
    start:   ['common_start_soft_01'],
    ready:   ['common_ready_soft_01'],
    half:    ['common_half_soft_01'],
    sec10:   ['common_10sec_soft_01'],
    sec5:    ['common_5sec_soft_01'],
    last:    ['common_last_soft_01'],
    end:     ['common_end_soft_01'],
    restart: ['common_restart_soft_01'],
    done:    ['common_done_soft_01'],
  },
  workout: {
    push: [
      'workout_push_energy_01',
      'workout_push_energy_02',
      'workout_push_energy_03',
      'workout_push_energy_04',
      'workout_push_energy_05',
    ],
    round:  ['workout_round_energy_01'],
    hold:   ['workout_hold_energy_01'],
    finish: ['workout_finish_energy_01'],
  },
  rest: {
    start:   ['rest_start_soft_01'],
    breathe: ['rest_breathe_soft_01'],
    half:    ['rest_half_soft_01'],
    ready:   ['rest_ready_soft_01'],
    end:     ['rest_end_soft_01'],
  },
  brush: {
    start: ['brush_start_kids_01'],
    switch: [
      'brush_top_kids_01',
      'brush_bottom_kids_01',
      'brush_left_kids_01',
      'brush_right_kids_01',
    ],
    end: ['brush_end_kids_01'],
  },
  reaction: {
    soft:   ['reaction_cheer_soft_01', 'reaction_cheer_soft_02'],
    energy: ['reaction_cheer_energy_01', 'reaction_cheer_energy_02'],
    kids:   ['reaction_kids_01', 'reaction_kids_02'],
  },
} as const

/** 배열에서 랜덤 1개 반환 */
export function pickRandom<T>(arr: readonly T[]): T {
  return arr[Math.floor(Math.random() * arr.length)]
}
```

---

### 3. `src/pages/RunSessionPage.tsx` 수정

인터벌 음성 로직의 `speak(텍스트)` 호출을 전부 `playOrSpeak`로 교체한다.

| 상황 | 파일 | fallback 텍스트 |
|------|------|-----------------|
| 휴식 시작 | `rest_start_soft_01.mp3` | 쉬는 시간이에요. |
| 휴식 종료 | `rest_end_soft_01.mp3` | 쉬는 시간 끝, 다시 갑니다. |
| 라운드 시작 | `workout_round_energy_01.mp3` | N라운드 시작! |
| 마지막 라운드 | `workout_round_energy_01.mp3` | 마지막 라운드! 끝까지 가자! |
| 운동 격려 | `workout_push_energy_01~05` 중 랜덤 | 더 버텨봐요! |
| 전체 완료 | `common_done_soft_01.mp3` | 모두 완료! 수고하셨습니다 |

> **라운드 번호 처리:** `workout_round_energy_01.mp3` 재생 성공 여부와 관계없이,
> `speak(String(intervalRound) + "라운드")` 를 추가로 호출해 숫자를 TTS로 읽는다.

---

### 4. `src/hooks/useBrushingScript.ts` 수정

양치 스크립트의 speak() 호출 지점을 `playOrSpeak`로 교체한다.

| 구간 | 파일 | fallback 텍스트 |
|------|------|-----------------|
| 시작 | `brush_start_kids_01.mp3` | 양치 시작해볼까요? |
| 위쪽 | `brush_top_kids_01.mp3` | 위쪽부터 닦아볼게요. |
| 아래쪽 | `brush_bottom_kids_01.mp3` | 아래쪽도 닦아볼까요? |
| 왼쪽 | `brush_left_kids_01.mp3` | 왼쪽도 깨끗하게요. |
| 오른쪽 | `brush_right_kids_01.mp3` | 오른쪽도 닦아볼게요. |
| 완료 | `brush_end_kids_01.mp3` | 우와, 양치 끝! |

---

### 5. `public/audio/` 폴더 생성

빈 폴더만 만든다. mp3 파일은 아직 없어도 된다.
`public/audio/.gitkeep` 파일 하나만 생성한다.

---

## 완료 조건

- [ ] `src/utils/audioPlayer.ts` 생성 완료
- [ ] `src/data/voiceFiles.ts` 생성 완료
- [ ] `public/audio/` 폴더 + `.gitkeep` 생성
- [ ] `RunSessionPage.tsx` 인터벌 음성 전부 `playOrSpeak`로 교체
- [ ] `useBrushingScript.ts` 양치 음성 `playOrSpeak`로 교체
- [ ] mp3 파일이 없는 상태에서 기존 TTS가 정상 동작하는지 확인
- [ ] `npm run lint && npm run build` 통과

---

## 나중에 할 것 (이번 작업 범위 아님)

- Google Neural2 / Azure TTS로 실제 mp3 파일 생성 후 `public/audio/`에 추가
- 코드 수정 없이 자동 적용됨
- v1.5: 같은 30개 문구를 energy 톤으로 추가 (`_energy_` 파일명)
- v2: kids / calm 톤 추가
- Pro 전용 고품질 파일 분리 (`/audio/pro/` 폴더)
